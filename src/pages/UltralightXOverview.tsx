import { Box } from "@mui/system";

const UltralightXOverview = () => {
  return (
    <Box
      sx={{
        height: "100vw",
        width: "100vh",
        background: "#000",
        color: "#fff",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      UltralightXOverview
    </Box>
  );
};

export default UltralightXOverview;
